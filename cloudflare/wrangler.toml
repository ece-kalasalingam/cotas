# Cloudflare Workers deployment config — deploy with: npx wrangler deploy
# Run locally:  npx wrangler dev cloudflare/worker.js

name = "cotas-cip"
main = "worker.js"
compatibility_date = "2024-09-23"

# Secret env vars — set with:
#   npx wrangler secret put GEMINI_API_KEY
#   npx wrangler secret put APP_TOKEN
# Never commit actual key values here.
